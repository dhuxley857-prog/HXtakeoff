"use client";
import{useMemo,useRef,useState}from"react";import{Upload,FileText,Ruler,ScanLine,CheckCircle2,Plus}from"lucide-react";
const items=[["W01","Internal partition","13.88 m²","K10/115","HIGH"],["W02","Acoustic partition","18.42 m²","K10/125","HIGH"],["W03","WC partition","6.20 m²","K10/135","MEDIUM"],["F01","Porcelain floor tile","28.40 m²","M40/110","HIGH"]];
export default function Home(){const[sel,setSel]=useState("W01");const[calibrated,setCalibrated]=useState(false);const footprint=useMemo(()=>9*5.4,[]);const[tool,setTool]=useState<"length"|"area"|"count"|"calibrate">("calibrate");const[mmPerPct,setMmPerPct]=useState(0);const[aiObjects,setAiObjects]=useState(false);const[pdfUrl,setPdfUrl]=useState<string|null>(null);const[pts,setPts]=useState<{x:number,y:number}[]>([]);const board=useRef<HTMLDivElement>(null);const addPoint=(e:React.MouseEvent)=>{if((!calibrated&&tool!=="calibrate")||!board.current)return;const r=board.current.getBoundingClientRect();const q={x:(e.clientX-r.left)/r.width*100,y:(e.clientY-r.top)/r.height*100};if(tool==="calibrate"){setPts(p=>{const n=[...p,q].slice(-2);if(n.length===2){const d=Math.hypot(n[1].x-n[0].x,n[1].y-n[0].y);const known=Number(prompt("Known distance between these points (mm)","9000"));if(known>0&&d>0){setMmPerPct(known/d);setCalibrated(true)}}return n})}else setPts(p=>[...p,q])};const reset=()=>setPts([]);const qty=useMemo(()=>{if(tool==="calibrate")return 0;if(tool==="count")return pts.length;if(pts.length<2)return 0;if(tool==="length"){let n=0;for(let i=1;i<pts.length;i++)n+=Math.hypot(pts[i].x-pts[i-1].x,pts[i].y-pts[i-1].y);return n*mmPerPct/1000}if(pts.length<3)return 0;let a=0;for(let i=0;i<pts.length;i++){const j=(i+1)%pts.length;a+=pts[i].x*pts[j].y-pts[j].x*pts[i].y}return Math.abs(a/2)*mmPerPct*mmPerPct/1e6},[pts,tool,mmPerPct]);
return <main><header><b><i>HX</i> TAKEOFF</b><span>TEST 001 · 32 Deanhill Road · RIBA Stage 4</span><em>DH</em></header><nav>Projects　 <strong>Drawings</strong>　 Documents　 Take-off　 BOQ</nav>
<section className="hero"><div><small>MEASUREMENT ENGINE · TEST 001</small><h1>Drawing intelligence workspace.</h1><p>Real Stage 4 construction information is used as the blind benchmark for document reading, geometry and take-off.</p></div><button><Plus size={17}/> New take-off</button></section>
<section className="steps">{[[Upload,"1. Drawing","CS-10-01 · Rev 2"],[Ruler,"2. Calibrate",calibrated?"9,000 × 5,400mm ✓":"Use known dimensions"],[ScanLine,"3. Measure","Deterministic geometry"],[CheckCircle2,"4. Validate","Benchmark 48.60m²"]].map(([I,a,b]:any,i)=><article key={i}><I/><b>{a}</b><span>{b}</span></article>)}</section>
<section className="work"><aside><h5>DRAWINGS</h5><div className="doc on"><FileText/> CS-10-01 Ground & First Floor Plan</div><h5>DOCUMENTS</h5><div className="doc"><FileText/> CS-00-01 General Notes</div><label className="upload" style={{display:"block",cursor:"pointer"}}>＋ Upload PDF<input type="file" accept="application/pdf" style={{display:"none"}} onChange={e=>{const file=e.target.files?.[0];if(file){if(pdfUrl)URL.revokeObjectURL(pdfUrl);setPdfUrl(URL.createObjectURL(file));setAiObjects(false);setPts([])}}}/></label><h5>AI DRAWING READ</h5><button className="upload" onClick={()=>setAiObjects(true)}>✦ Analyse test drawing</button>{aiObjects&&<p style={{fontSize:12,lineHeight:1.5}}>✓ Stage 4 sheet identified<br/>✓ Drawing/revision metadata queued<br/>✓ Figured dimensions prioritised<br/>✓ Construction notes queued</p>}<h5>CALIBRATION</h5><p style={{fontSize:12,lineHeight:1.5}}>Use figured dimensions from the Stage 4 drawing for calibration. Do not assume scale.</p><button className="upload" onClick={()=>{setTool("calibrate");setPts([]);setCalibrated(false)}}>{calibrated?`✓ Scale ${mmPerPct.toFixed(1)} mm/unit`:"Start 2-point calibration"}</button></aside>
<div className="viewer"><div className="bar">TEST 001 · Native drawing workspace <span>Stage 4 · measurement overlay</span></div><div style={{padding:"8px",display:"flex",gap:6,flexWrap:"wrap",alignItems:"center"}}>{(["calibrate","length","area","count"] as const).map(t=><button key={t} onClick={()=>{setTool(t);reset()}} style={{opacity:tool===t?1:.55}}>{t.toUpperCase()}</button>)}<button onClick={reset}>CLEAR</button><span style={{marginLeft:"auto",fontWeight:700}}>{tool==="calibrate"?(calibrated?"SCALE VALID":"PICK 2 POINTS"):tool==="count"?pts.length+" nr":qty.toFixed(2)+(tool==="area"?" m²":" m")}</span></div><div ref={board} onClick={addPoint} style={{position:"relative",background:"#f8f8f5",height:"70vh",minHeight:560,overflow:"hidden",cursor:(calibrated||tool==="calibrate")?"crosshair":"default"}}>{pdfUrl?<object data={pdfUrl} type="application/pdf" style={{position:"absolute",inset:0,width:"100%",height:"100%",border:0}}><p>PDF preview is not supported in this browser.</p></object>:<><div style={{position:"absolute",inset:"5%",background:"white",border:"1px solid #c9ced2",boxShadow:"0 3px 18px #0001"}}><div style={{position:"absolute",left:"8%",top:"10%",right:"8%",bottom:"12%",border:"5px double #26333d"}}><div style={{position:"absolute",left:"0",top:"52%",width:"58%",borderTop:"5px solid #26333d"}}/><div style={{position:"absolute",left:"58%",top:"0",height:"100%",borderLeft:"5px solid #26333d"}}/><div style={{position:"absolute",left:"78%",top:"0",height:"42%",borderLeft:"4px solid #26333d"}}/><div style={{position:"absolute",left:"12%",top:"22%",fontSize:13}}>KITCHEN / DINING</div><div style={{position:"absolute",left:"14%",top:"72%",fontSize:13}}>LIVING</div><div style={{position:"absolute",left:"65%",top:"25%",fontSize:13}}>HALL</div><div style={{position:"absolute",left:"82%",top:"22%",fontSize:13}}>WC</div><div style={{position:"absolute",left:"68%",top:"70%",fontSize:13}}>STUDY</div></div><div style={{position:"absolute",left:"8%",bottom:"4%",fontSize:10}}>TEST 001 · STAGE 4 DRAWING WORKSPACE · VISUAL TEST SHEET</div></div></>}<svg viewBox="0 0 100 100" preserveAspectRatio="none" style={{position:"absolute",inset:0,width:"100%",height:"100%",pointerEvents:"none"}}>{aiObjects&&<g><rect x="12" y="15" width="43" height="35" fill="rgba(198,162,75,.12)" stroke="#c6a24b" strokeWidth=".5"/><rect x="12" y="51" width="43" height="31" fill="rgba(198,162,75,.12)" stroke="#c6a24b" strokeWidth=".5"/><line x1="55" y1="15" x2="55" y2="82" stroke="#c6a24b" strokeWidth=".7"/><text x="14" y="19" fontSize="2.2" fill="#8b6d24">AI AREA</text><text x="57" y="19" fontSize="2.2" fill="#8b6d24">AI WALL</text></g>}{pts.length>1&&<polyline points={pts.map(p=>p.x+","+p.y).join(" ")} fill={tool==="area"?"rgba(45,112,88,.15)":"none"} stroke="#2d7058" strokeWidth=".6"/>}{pts.map((p,i)=><circle key={i} cx={p.x} cy={p.y} r=".7" fill="#2d7058"/>)}</svg></div><div style={{padding:8,fontSize:11,opacity:.65}}>Upload a PDF from your device to replace the visual test sheet. The markup layer remains in the same workspace so measurements can be drawn over the document.</div></div>
<aside className="take"><h5>TAKE-OFF EVIDENCE　 <mark>{calibrated?"SCALE VALID":"REVIEW"}</mark></h5>{aiObjects&&<div className="item picked"><div><b>AI drawing interpretation</b><small>Geometry + tags + dimension evidence</small></div><strong>REVIEW</strong></div>}<div className="item picked"><div><b>Benchmark footprint</b><small>Blind test · no benchmark quantity exposed to intelligence</small></div><strong>REVIEW</strong></div>{items.map(x=><div key={x[0]} onClick={()=>setSel(x[0])} className={"item "+(sel===x[0]?"picked":"")}><div><b>{x[0]} · {x[1]}</b><small>Spec {x[3]} · Evidence {x[4]}</small></div><strong>{x[2]}</strong></div>)}<footer><button>Reject</button><button>Amend</button><button className="approve">Approve</button></footer></aside></section></main>}